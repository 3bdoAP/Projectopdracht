entryPoints:
  web:
    address: ":80"
    http:

  websecure:
    address: ":443"

certificatesResolvers:
  letsencrypt:
    acme:
      email: john.doe@example.org
      storage: acme.json
      httpChallenge:
        entryPoint: web
providers:
  docker: {}
  file:
    directory: "/etc/traefik/conf.d"
api:
  dashboard: true
